# Development and test dependencies.
#
#     pip install -r requirements-dev.txt
#
# Installs production dependencies too, so a development environment always
# runs the same versions production does.
#
# None of this reaches the image: the Dockerfile installs requirements.txt
# only. That is the point of the split -- pytest in a production container is
# code an attacker can reach that serves no user.
-r requirements.txt

# Migrations are an operator activity run from a laptop against RDS, never
# from the Lambda -- see requirement 6 and the runbook. Alembic therefore does
# not belong in the image, so it lives here with Mako and MarkupSafe, which
# exist only to render its migration templates.
alembic==1.18.5
Mako==1.3.12
MarkupSafe==3.0.3

# Test runner and its dependencies
pytest==9.1.1
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0

# Starlette's TestClient is built on httpx
certifi==2026.7.22
httpcore==1.0.9
httpcore2==2.9.1
httpx==0.28.1
httpx2==2.9.1
truststore==0.10.4

# uvicorn[standard] extras that only matter locally: watchfiles powers
# --reload, websockets is unused (no WebSocket endpoints), and PyYAML is only
# read for --log-config files.
watchfiles==1.2.0
websockets==17.0.1
PyYAML==6.0.3
