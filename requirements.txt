# Production dependencies only. This is what goes into the image.
#
# Test and development tooling lives in requirements-dev.txt. Anything added
# here ships to production and becomes attack surface, so the bar for adding
# a line is "the running application imports this".
#
# Versions are pinned to exactly what the test suite runs against. Production
# must execute the same code that was tested, so this file and
# requirements-dev.txt never disagree about a shared package's version.
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
bcrypt==5.0.0
click==8.4.2
dnspython==2.8.0
email-validator==2.3.0
fastapi==0.141.1
greenlet==3.5.4
h11==0.16.0
httptools==0.8.0
idna==3.18
mangum==0.21.0
psycopg==3.3.4
psycopg-binary==3.3.4
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
PyJWT==2.13.0
python-dotenv==1.2.2
python-multipart==0.0.32
SQLAlchemy==2.0.51
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.52.1
uvloop==0.22.1
